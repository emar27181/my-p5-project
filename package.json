{
  "private": true,
  "scripts": {
    "dev": "parcel views/index.html --open",
    "build": "parcel build views/index.html",
    "start": "parcel views/index.html --open && parcel build views/index.html",
    "restart": "rm -rf .parcel-cache && parcel views/index.html --open && parcel build views/index.html",
    "auto-rebase": "git checkout main && git pull && git checkout edit && git rebase main"
  },
  "devDependencies": {
    "@types/p5": "^1.4.3",
    "parcel": "^2.8.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "p5": "^1.5.0"
  }
}
